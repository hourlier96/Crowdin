{% extends 'base.html.twig' %}

{% block body %}
</br>
    <div class="jumbotron">
        <h3>Change your password</h3>
        </br>
        {{ form_start(form_pwd) }}
        {{ form_row(form_pwd.password, {'label': 'New Password'}) }}
        {{ form_row(form_pwd.submit, {'label': 'Change'})}}
        {{ form_end(form_pwd) }}
    </div>

{% endblock %}